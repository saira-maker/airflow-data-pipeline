name: Deploy DAGs to Astro

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy DAGs to Astro
        uses: astronomer/deploy-action@v0.3.3

        with:
          deployment-id: ${{ secrets.ASTRO_DEPLOYMENT_ID }}
          action: deploy
          root-folder: "."
          deploy-image: false
          image-name: "no-custom-image"
          deploy-type: dags-only            # ✅ This fixes the line 65 bash error
          copy-connections: true
          copy-airflow-variables: true
          copy-pools: true
          build-secrets: |
            ASTRO_API_TOKEN=${{ secrets.ASTRO_API_TOKEN }}
